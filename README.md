# pbcopy-in-Rust

```bash
➜  pbcopy git:(master) ✗ echo "hello" | cargo run -- -h
    Finished dev [unoptimized + debuginfo] target(s) in 0.0 secs
     Running `target/debug/pbcopy -h`
Rpbcopy 0.1.0
Bradley and Liuda
pbcopy clone written in Rust

USAGE:
    pbcopy

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information
```
